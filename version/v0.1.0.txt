v0.1.0
    改进：
        1.区分角色：骑士&Bear
        2.添加了propView(属性窗口）
        3.添加了introduce(游戏简介）
        4.修改了knight(骑士)的AI和属性
        5.更改了传输数据的格式(现在包括了propInfo和moveInfo)
        6.简化了用户操作
    问题：
        1.会卡死
        2.cache图片的尺寸太小，导致放大后锯齿严重

v0.1.1
    改进：
        1.改进了knight（骑士）的AI
        2.完善了攻击反馈机制
        2.修正了方向键操作相反的问题
        3.修正了cache图片的尺寸（从20*20 到 50*50）
    问题：
        1.目标对象判断有问题（这也是为什么明明碾压过去却不能杀死目标的原因）
        2.护甲的重要性应该与攻击力相若

v0.1.2
    改进：
        1.改进了Bear(熊)的AI（添加了护甲相关属性）
        2.在展示界面添加了护甲属性。
    问题：
        1.速度可以为负数。而且相当快。。。。
        2.没有进入游戏，却可以向游戏输入命令
        3.需要添加服务器的数据自动检测和维护功能
        4.python

v0.1.3
    改进：
        1.添加了阵营（同阵营之间可以组队，只能进攻不同阵营）
        2.添加了指定路径属性（对象将以指定属性行动）
    问题：
        1.连续点击“加入游戏”会获得多个控制权限

v0.1.4
    改进：
        1.添加了房间机制
        2.添加了对房间的操作（增加，删除）
        3.修改了多次加入游戏的bug
    问题：
        1.方块（设置头部）与三角的颜色需要更改
    ！  2.自适应绘图区的大小
    ！  3.观战模式（围观游戏）
    ！  4.控制问题

v0.1.5
    改进：
        1.修正了addSprite的逻辑错误
        2.添加了darkTower 和orc 两个对象
        3.添加了命令模式
        4.完成了绘图区屏幕的自适应
    问题：
        1.orc的view需要修改
        2.加入游戏界面选择角色

v0.1.6
    改进：
        1.修改了refreshGeo的算法，提升了计算速度
        2.修改了绘制区的大小
        3.添加了角色选择选项
    问题：
        1.更新刷新按钮
        2.you are already in BUG
        3.DarkTower的camp属性与Bear不一致

v0.1.7
    改进：
        1.添加了client对象
        2.添加了UNI_id
        3.添加了id显示
    问题：
        1.client控制权限混乱
        2.新加入的控制者ctrName不能显示
        3.缩放问题
        4.没有ID也可以进游戏。。。

v0.1.8
    改进：
        1.修复了v0.1.7的主要问题
        2.关闭游戏需要经过GM,不能直接stopMachine
        3.将逻辑和分发分离~

v0.1.9
    改进：
        1.优化了前端展示（去掉了数据相关项）
